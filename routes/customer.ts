import express, {Router, Request, Response, NextFunction} from "express";


const router = Router();

router.post("/signup")
router.post("/login")

router.patch("/verify")
router.get("/otp")





export {router as customer};